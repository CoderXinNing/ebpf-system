星轨页时间线 - 待优化项
=================================================

现状：勉强能用（2026-09-24）
- 进程时间线 tab 已上线
- execve 显示真实命令

待优化：
1. 数据格式规范
   - 现状：execve 的 filename 是占位符 "execve"，真实命令塞 details
   - 目标：filename 填真实 exe 路径，details 填 cmdline
   - 影响：需改 Agent 上报逻辑 + 兼容旧数据

2. 时间线展示
   - 154 个进程 / 271 个事件 → 首屏加载慢
   - 加"仅显示高危进程"筛选
   - 加"按时间排序"（现在按 PID 分组，时间维度弱）
   - 折叠后显示时间范围

3. 进程树关系
   - 现在按 PID 平铺，看不出父子关系
   - 加 PPID 关联（拉出进程树视图）

4. 事件内容
   - 长命令截断（现在 80 字符）
   - 加"复制"按钮
   - JSON 美化（如果 details 是结构化数据）

5. 高危事件高亮
   - tcp_connect/bash_input 已算 alertCount
   - 前端加视觉强调（红色边框）

优先级：低（非阻塞）
